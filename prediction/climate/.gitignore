__pycache__/
*.pyc
*.pyo
.env

# Model artifacts: track seed files for deployment; gitignore bulk data only
# models_store/ — removed so Render can deploy with a seed model

# Climate data cache
/tmp/climate_data/
